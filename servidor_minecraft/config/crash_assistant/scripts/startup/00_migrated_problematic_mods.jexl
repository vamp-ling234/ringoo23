var allMods = ModListUtils.getCurrentModList(true);
var mods = ModListUtils.getCurrentModListMappedToModId();

var m_essential_container = mods.get("essential-container");
if (m_essential_container != null) {
    var warn = Startup.addCrashWarning("⚠️ <strong>Essential Mod Not Supported</strong>\n\nWe strongly recommend against using Essential (<strong>" + m_essential_container.jarName + "</strong>) with this modpack. <strong>A free world-sharing alternative is already built-in</strong> using e4mc - click <strong>Share World</strong> in the game menu.\n\n<strong>Why we don't support Essential:</strong>\n• <strong>Technical issues:</strong> We've received multiple user reports of problems when combining Essential with our modpacks, including reports of world corruption. While there is no conclusive evidence of Essential as the definite cause, we do not have the development or support capacity to resolve issues potentially caused by it.\n\n• <strong>Security concerns:</strong> Essential's auto-updater introduces potential security risks that are outside of our control.\n\n• <strong>Ethics:</strong> We object to Essential's inclusion of in-game currency and microtransactions in an otherwise free modding environment.\n\n<strong>If you choose to use Essential anyway</strong>, please understand that you do so at your own risk and will not be eligible for support from our team. To remove this warning, you can open your modpack instance folder, navigate to <strong>config/crash_assistant/scripts/startup/00_migrated_problematic_mods.jexl</strong>, and remove the sections for essential.");
    warn.withModActions(m_essential_container);
    Startup.markForCrash();
}

var m_e4mc_minecraft = mods.get("e4mc_minecraft");
if (m_e4mc_minecraft != null) {
    var warn = Startup.addCrashWarning("⚠️ <strong>Duplicate e4mc Mod Detected</strong>\n\nYou have manually added the <strong>e4mc</strong> mod (<strong>" + m_e4mc_minecraft.jarName + "</strong>) to the modpack. This is not needed, as <strong>all Luna Pixel Studios modpacks already include our custom fork of e4mc by default</strong> with built-in world sharing functionality.\n\n<strong>Why this causes issues:</strong>\n• Having both versions installed can cause conflicts and unexpected behavior\n• Our custom fork includes optimizations and fixes specific to our modpacks\n\n<strong>Recommended action:</strong> Remove the manually added e4mc mod from your mods folder.\n\n<strong>Alternative:</strong> If you are having issues with our built-in world sharing, hosting a server on your own PC via the serverpack is easy. Learn more here: https://github.com/chorbintime/LPS-readme/wiki\n\n<strong>If you still want to proceed with the duplicate mod</strong>, please understand that you do so at your own risk and will not be eligible for support from our team. To remove this warning, you can open your modpack instance folder, navigate to <strong>config/crash_assistant/scripts/startup/00_migrated_problematic_mods.jexl</strong>, and remove the sections for e4mc_minecraft.");
    warn.withModActions(m_e4mc_minecraft);
    Startup.markForCrash();
}

var m_tlskincape = mods.get("tlskincape");
if (m_tlskincape != null) {
    var warn = Startup.addCrashWarning("It appears that you are using <strong>TLauncher</strong>, a launcher likely to be infected with spyware that could steal your passwords and monitor your computer activity. <strong>TLauncher</strong> also does not install modpacks correctly, which results in many missing files that will potentially break the modpack. It is heavily encouraged to delete all traces of TLauncher off of your computer, do an antivirus scan, and buy Minecraft so you can use a better launcher, such as https://atlauncher.com/downloads.");
    warn.withModActions(m_tlskincape);
    Startup.markForCrash();
}

var m_despawn_tweaker = mods.get("despawn_tweaker");
if (m_despawn_tweaker != null) {
    var warn = Startup.addCrashWarning("If you are seeing this, you have manually added the <strong>Despawn Tweaker</strong> mod to the modpack. The <strong>Despawn Tweaker</strong> mod is not needed, as a better mod with similar functionality (<strong>Despawn Tweaks</strong>) is already present in the modpack.");
    warn.withModActions(m_despawn_tweaker);
    Startup.markForCrash();
}

var m_doespotatotick = mods.get("doespotatotick");
if (m_doespotatotick != null) {
    var warn = Startup.addCrashWarning("If you are seeing this, you have manually added the <strong>DoesPotatoTick</strong> mod to the modpack. The <strong>DoesPotatoTick</strong> mod is not needed, as a better mod with similar functionality (<strong>Does It Tick</strong>) is already present in the modpack.");
    warn.withModActions(m_doespotatotick);
    Startup.markForCrash();
}

var m_dynamiclights = mods.get("dynamiclights");
if (m_dynamiclights != null) {
    var warn = Startup.addCrashWarning("If you are seeing this, you have manually added the <strong>Dynamic Lights mod</strong> to the modpack. The <strong>Dynamic Lights</strong> mod is not needed, as a better mod with similar functionality (either <strong>Sodium/Embeddium Dynamic Lights</strong> or <strong>LambDynamicLights</strong>) is already present in the modpack.");
    warn.withModActions(m_dynamiclights);
    Startup.markForCrash();
}

var m_epicfight = mods.get("epicfight");
if (m_epicfight != null) {
    var warn = Startup.addCrashWarning("⚠️ <strong>Epic Fight Mod Not Supported</strong>\n\nYou have manually added the <strong>Epic Fight</strong> mod (<strong>" + m_epicfight.jarName + "</strong>) to the modpack. We strongly recommend against using this mod.\n\n<strong>Why we don't support Epic Fight:</strong>\n• <strong>Stability issues:</strong> Epic Fight is known to cause frequent crashes and game-breaking bugs\n• <strong>Compatibility problems:</strong> Epic Fight has widespread incompatibilities with many mods commonly found in modpacks\n• <strong>Support limitations:</strong> We do not have the development capacity to troubleshoot issues caused by Epic Fight's extensive modifications\n\n<strong>Recommended alternative:</strong> If you would like a more stable combat overhaul, consider using the <strong>Better Combat</strong> mod instead.\n\n<strong>If you choose to use Epic Fight anyway</strong>, please understand that you do so at your own risk and will not be eligible for support from our team. To remove this warning, you can open your modpack instance folder, navigate to <strong>config/crash_assistant/scripts/startup/00_migrated_problematic_mods.jexl</strong>, and remove the sections for epicfight.");
    warn.withModActions(m_epicfight);
    Startup.markForCrash();
}

var m_valkyrienskies = mods.get("valkyrienskies");
if (m_valkyrienskies != null) {
    var warn = Startup.addCrashWarning("⚠️ <strong>Valkyrien Skies Not Supported</strong>\n\nYou have manually added the <strong>Valkyrien Skies</strong> mod (<strong>" + m_valkyrienskies.jarName + "</strong>) to the modpack. We strongly recommend against using this mod with pre-existing modpacks.\n\n<strong>Why we don't support Valkyrien Skies:</strong>\n• <strong>Compatibility issues:</strong> Valkyrien Skies makes fundamental changes to physics and world handling that conflict with many other mods\n• <strong>Integration requirements:</strong> Valkyrien Skies requires careful modpack design and testing from the ground up to work properly\n• <strong>Technical limitations:</strong> Adding Valkyrien Skies to an existing modpack that wasn't designed for it typically results in crashes, world corruption, and unpredictable behavior\n\n<strong>If you choose to use Valkyrien Skies anyway</strong>, please understand that you do so at your own risk and will not be eligible for support from our team. To remove this warning, you can open your modpack instance folder, navigate to <strong>config/crash_assistant/scripts/startup/00_migrated_problematic_mods.jexl</strong>, and remove the sections for valkyrienskies.");
    warn.withModActions(m_valkyrienskies);
    Startup.markForCrash();
}

var m_optifine = mods.get("optifine");
if (m_optifine != null) {
    var warn = Startup.addCrashWarning("⚠️ <strong>OptiFine Not Compatible</strong>\n\nYou have manually added the <strong>OptiFine</strong> mod (<strong>" + m_optifine.jarName + "</strong>) to the modpack.\n\n<strong>Why OptiFine doesn't work with this pack:</strong>\n• <strong>Compatibility issues:</strong> OptiFine is closed-source and makes extensive, undocumented changes to Minecraft's rendering code, causing conflicts with many mods in this pack\n• <strong>Better alternatives included:</strong> Modern optimization and shader mods (Sodium/Xenon and Iris/Oculus) are already present in this modpack and provide superior performance with full mod compatibility\n\n<strong>Recommended action:</strong> Remove OptiFine from your mods folder.");
    warn.withModActions(m_optifine);
    Startup.markForCrash();
}